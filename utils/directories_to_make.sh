#!/bin/bash

export DIRECTORIES_TO_CREATE=(
    "$HOME/bin"
    "$HOME/.ssh"
    "$HOME/.ssh/config.d"
    "$HOME/.nvm"
    "$HOME/Documents"
    "$HOME/Downloads"
    "$HOME/repos"
    "$XDG_CONFIG_HOME"
    "$XDG_DATA_HOME"
    "$XDG_CACHE_HOME"
    "$ZDOTDIR"
    "$COOKIECUTTERDIR"
)
